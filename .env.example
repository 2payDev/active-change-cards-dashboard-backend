# Use IP if hostname DNS fails intermittently
DB_HOST=103.65.248.209
# DB_HOST=digital1.vita.com.pk
DB_PORT=5433
DB_NAME=work
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# Comma-separated frontend URLs (required on Vercel)
ALLOWED_ORIGINS=http://localhost:5173,https://active-change-cards-dashboard-front.vercel.app
